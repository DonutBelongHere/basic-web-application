%h1 Listing songs

%table
  %thead
    %tr
      %th Album
      %th Song title
      %th Genre
      %th Length
      %th{:colspan => "3"}= 'Actions'

  %tbody
    - @songs.each do |song|
      %tr
        %td= song.album.album_title
        %td= song.song_title
        %td= song.genre
        %td= song.length
        %td= link_to t('.show_song'), song
        %td= link_to t('.edit_song'), edit_song_path(song)
        %td= link_to t('.delete_song'), song, method: :delete, data: { confirm: 'Are you sure?' }

%br

%div{:class => "button"}= link_to t('.add_song'), new_song_path